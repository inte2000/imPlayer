
在 AudioSource.cpp 中新增一个 MakeNetStreamAudioSource 函数：
std::unique_ptr<CAudioSource> MakeNetStreamAudioSource(const std::wstring& url)

根据 url 的前缀是否是 http 或 https 判断是否是 http 类型，对于不符合的情况，暂时都按照异常处理，异常信息是：“only support http stream”。这个函数暂时用来测试 NetStream 能否和解码器配合使用，所以 factory.MakeAudioDecoder 使用 StreamFormatMp3（因为测试的 http 流是 mp3 格式）。

新增一个 CNetStreamMusic 类：

class CNetStreamMusic : public CMusic

CNetStreamMusic::MakeAudioSource() 函数调用 MakeNetStreamAudioSource 

/***********************************************************/

已按最新 todo_task.txt 完成实现并验证。
- 新增 MakeNetStreamAudioSource  
在 include/player/AudioSource.h 声明、src/player/AudioSource.cpp 实现：  
- URL 前缀为 http:// 或 https:// 才允许；  
- 不符合时抛出异常：only support http stream；  
- 通过 MakeNetStream(url, NetStreamType::Http) 创建流；  
- 解码器固定使用 factory.MakeAudioDecoder(StreamFormatMp3)（按你的测试约束）。
- 新增 CNetStreamMusic : public CMusic  
在 include/player/Music.h 增加类声明，在 src/player/Music.cpp 完整实现，MakeAudioSource() 调用 MakeNetStreamAudioSource(m_item.res_url)。
- 播放列表接入网络流类型  
在 src/player/PlayList.cpp 中将 MUSIC_ITEM_TYPE_NETWORK_STREAM 路由为 std::make_unique<CNetStreamMusic>(item)。
- 测试适配  
更新 test/player/PlayerTests.cpp：原“网络流为不支持类型”用例改为验证可创建 CMusic 对象并返回 MUSIC_ITEM_TYPE_NETWORK_STREAM 与 URL（不触发真实网络连接）。
验证结果：  
- cmake --build build/debug --target imPlayer：通过。  
- cmake --build build/test --target imPlayerTests：仍被仓库既有 test/core/StringExTests.cpp 历史错误阻断（C2001/C1057），但本次改动涉及的 PlayerTests.cpp 已进入编译流程，无新增编译错误。

